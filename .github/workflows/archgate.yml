name: archgate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  _archgate-self-hosted:
    name: "archgate (self-hosted)"
    runs-on: [self-hosted, ubuntu-latest]
    continue-on-error: true
    outputs:
      passed: ${{ steps.mark.outputs.passed }}
    steps:
      - uses: actions/checkout@v4
      - name: Route unsupported self-hosted archgate runtime
        if: runner.os == 'Linux' && runner.arch != 'X64'
        run: echo "archgate setup supports Linux x86_64 only; falling back to canonical check"
      - name: Setup archgate
        if: runner.os != 'Linux' || runner.arch == 'X64'
        # archgate v0.43+ ships a bundled TypeScript runtime (bun + node),
        # so no `actions/setup-node@v4` step is needed.
        # archgate-runtime: bundled
        uses: archgate/setup-action@v1
      - name: archgate check
        if: runner.os != 'Linux' || runner.arch == 'X64'
        # archgate-runtime: bundled
        run: archgate check
      - id: mark
        if: success() && (runner.os != 'Linux' || runner.arch == 'X64')
        run: echo "passed=true" >> "$GITHUB_OUTPUT"

  archgate:
    name: "archgate"
    needs: _archgate-self-hosted
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Route
        run: echo "self-hosted passed=${{ needs._archgate-self-hosted.outputs.passed }}"

      - if: needs._archgate-self-hosted.outputs.passed != 'true'
        uses: actions/checkout@v4

      - name: Setup archgate
        if: needs._archgate-self-hosted.outputs.passed != 'true'
        # archgate-runtime: bundled
        uses: archgate/setup-action@v1

      - name: archgate check
        if: needs._archgate-self-hosted.outputs.passed != 'true'
        # archgate-runtime: bundled
        run: archgate check
