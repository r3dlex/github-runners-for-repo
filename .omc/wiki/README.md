# Wiki

This directory holds OMC wiki pages (architecture notes, debugging guides,
session logs) that are version-controlled with the rest of the project.

State-only wiki data is kept in `.omc/state/`, which is gitignored.
