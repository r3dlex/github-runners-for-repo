Test commit for the pr-issue-link workflow — this branch intentionally
omits `Closes #N` so the pr-issue-link job fails. Tracked file is
gitignored for the test PR (added via .gitignore exemption here so
it shows in the diff).
